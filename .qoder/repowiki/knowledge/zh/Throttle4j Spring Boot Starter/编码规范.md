- 所有自动配置的 Bean 均使用 `@ConditionalOnMissingBean` 保护，允许用户自定义覆盖。
- AOP 切面与拦截器均通过构造函数注入 `RateLimiterRegistry` 和配置属性，确保状态共享。
- 限流键（Key）解析支持 SpEL 表达式，并在 `RateLimitAspect` 中通过 `ConcurrentHashMap` 缓存解析后的表达式以提升性能。