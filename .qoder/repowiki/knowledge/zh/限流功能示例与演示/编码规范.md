- REST 控制器中通过 `@RateLimit` 注解声明式地配置限流参数（limit, window, algorithm）。
- 使用 `@RestControllerAdvice` 集中处理限流异常，将其转换为标准的 HTTP 429 响应及 Retry-After 头。
- 核心 API 演示采用 Builder 模式 (`RateLimiterConfig.builder()`) 构建限流配置，并通过工厂 (`RateLimiterFactory`) 创建实例。