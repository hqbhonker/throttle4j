- 所有 Lua 脚本均放置在 `src/main/resources/scripts/` 目录下，并在类加载时通过 `loadScript` 方法读取为字符串缓存。
- 限流算法的执行结果统一封装为 `List<Object>`，并通过辅助方法 `longAt` 解析为具体的状态值（allowed, remaining, resetAt/retryAfter）。
- 对外暴露的 `tryAcquire` 方法在执行前均对 key、permits 和 config 进行非空及合法性校验。